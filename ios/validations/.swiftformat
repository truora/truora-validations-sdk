# SwiftFormat Configuration
# https://github.com/nicklockwood/SwiftFormat

# Exclude third-party dependencies and generated files
--exclude TensorFlowLite,TensorFlowLiteSwift,Derived

# Align with SwiftLint's trailing_comma rule (no trailing commas)
--trailing-commas never

# Match SwiftLint indentation
--indent 4
--indentcase false
--trimwhitespace always
--xcodeindentation enabled
--ifdef no-indent

# Swift version - updated for 2026 best practices
--swiftversion 5.10

# Disable rules that might conflict with SwiftLint
--disable redundantSelf,wrapMultilineStatementBraces

# Additional formatting preferences
--semicolons never
--commas inline
--stripunusedargs closure-only
--self remove
--importgrouping testable-bottom
--patternlet inline
--elseposition same-line
--guardelse same-line
--wraparguments before-first
--wrapparameters before-first
--wrapcollections before-first
--closingparen balanced
--funcattributes preserve
--typeattributes preserve
--varattributes preserve

# Organized imports - enabled
--enable sortImports

# Explicit return for single-expression functions (Swift 5.1+)
--enable redundantReturn

# Mark unused arguments (matches SwiftLint)
--enable unusedArguments
