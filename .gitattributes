# XCFramework zip files are large binary files, so we use Git LFS to handle them efficiently.
# Track XCFramework zip files in the XCFrameworks directory
ios/validations/XCFrameworks/*.xcframework.zip filter=lfs diff=lfs merge=lfs -text
